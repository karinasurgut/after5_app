<% provide(:title, "Wishlist") %>
<div class="row">	  
  <div class="col-lg-6 center">
    <%= userpic_for(@user,"large") %><br/>
    <h1 class="center">
      <%= @user.name %>
    </h1>
  </div> 
  <div class="col-lg-6">
	<h3>Deals you want to try</h3>
  	<ul class="deals">
  		<% @deals = current_user.find_liked_items %>
  		<% @deals.each do |deal| %>
    		<%= render partial: 'deals/deal', locals: {deal: deal} %>
  		<% end %>
	</ul>
  </div>
</div>